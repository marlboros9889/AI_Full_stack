<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.the703.dao.Sboard2Dao">

    <!-- 글쓰기 -->
    <insert id="insert" parameterType="Sboard2Dto">
        insert into sboard2
        (ID, APP_USER_ID, BTITLE, BCONTENT, BPASS, BFILE, BIP)
        values
        (sboard2_seq.nextval, #{appUserId}, #{btitle}, #{bcontent}, 
         #{bpass}, #{bfile, jdbcType=VARCHAR}, #{bip})
    </insert>

    <!-- 전체목록 -->
    <select id="selectAll" resultType="Sboard2Dto">
        select * from sboard2 order by id desc
    </select>

    <!-- 페이징 목록 -->
    <select id="selectpaging" resultType="Sboard2Dto" parameterType="java.util.HashMap">
        SELECT * FROM sboard2 ORDER BY id DESC
        OFFSET #{start} ROWS FETCH NEXT #{end} ROWS ONLY
    </select>

    <!-- 총 갯수 -->
    <select id="selectCnt" resultType="int">
        select count(*) from sboard2
    </select>

    <!-- 상세조회 -->
    <select id="selectById" resultType="Sboard2Dto">
        select * from sboard2 where id=#{id}
    </select>

    <!-- 조회수 증가 -->
    <update id="updateHit" parameterType="Sboard2Dto">
        update sboard2 set bhit = bhit + 1 where id=#{id}
    </update>

    <!-- 수정 (비밀번호 확인) ✅ -->
    <update id="update" parameterType="Sboard2Dto">
        update sboard2
        set btitle=#{btitle}, bcontent=#{bcontent}, 
            bfile=#{bfile, jdbcType=VARCHAR}
        where id=#{id}
        AND bpass=#{bpass}
    </update>

    <!-- 삭제 (비밀번호 확인) ✅ -->
    <delete id="delete" parameterType="Sboard2Dto">
        delete from sboard2
        where id=#{id}
        AND bpass=#{bpass}
    </delete>

</mapper>