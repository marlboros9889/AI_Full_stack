package com.the703.basic011_ex;

import java.util.Scanner;

public class MethodEx004 {

	public static int process_total(int kor, int eng, int math) {
		return kor + eng + math;
	}

	public static float process_avg(int total) {
		return total / 3f;
	}
//	public static float process_avg(int total){ total/3f; }

	public static String process_pass(float avg, int kor, int eng, int math) {
		return (boolean) (avg >= 60 || kor >= 40 || eng >= 40 || math >= 40) ? "합격" : "불합격";
	}
//  public static String process_pass(float avg ,int kor,int eng,int math){ avg>=60, kor>=40, eng>=40,math>=40; }

	public static String process_scholar(float avg){ if (avg<=95) {return "장학생";} else {return "---";} }
	
	public static String process_star(int avg){String total = null;
		 					         if  (avg<=70)  {total = "★"+"*7"+"개";}
		 					         if	 (avg<=80)  {total ="★"+"*8"+"개";}; 
		 						     if	 (avg<=90)  {total ="★"+"*9"+"개";}; 
		 						     if     (avg<=100) {total ="★"+"*10"+"개";};
									 return total;
								              };
	
	
	public static void main(String[] args) {
		// public static 리턴값 메서드명(재료){ 할일 }
/////////////////////(1)  변수
		String name = " ";
		int kor, eng, math, total;
		float avg = 0.0f;
		String pass = " ";
		String jang = " ";
		String star = " ";
		Scanner sc = new Scanner(System.in);
		/////////////////////////(2) 입력 : 이름, 국어, 영어, 수학점수를 입력받으시오.
		//		System.out.println("이름을 입력하세요");
		//		name = sc.next();
		//		System.out.println("국어점수를 입력하세요");
		//		kor = sc.nextInt();
		//		System.out.println("영어점수를 입력하세요");
		//		eng = sc.nextInt();
		//		System.out.println("수학점수를 입력하세요");
		//		math = sc.nextInt();
		
		name="아이언맨";  kor=100; eng=100; math=99;
		///////////////////////(3) 처리 : 
		total = process_total(kor, eng, math); // 1. 총점처리
		//  // public static int process_total(int kor+int eng+int math){ retrun kor+eng+math; }

		avg = process_avg(total); // 2. 평균처리
		//  public static float process_avg(int total){ total/3f; }

		//////////3.  합격  평균이60이상이고, 각각 국어, 영어, 수학40이상/불합격/재시험-각각 40미만인게 있다면  
		pass = process_pass(avg, kor, eng, math);
		//  public static String process_pass(float avg ,int kor,int eng,int math){ avg>=60, kor>=40, eng>=40,math>=40; }

		////////// 4. 평균이 95점이상이면 장학생
		jang = process_scholar(  avg  ); 
		//// 	public static String process_scholar( double avg  ){avg 95<="장학생"} 

		// 5. 평균점수대로 별표 붙이기 , 예) 70점대이면 별7개, 80점대이면 별8개, 90점대이면 별9개 , 100점이면 별10개 
        //star = process_star(avg);  
		// public static String process_star(avg){ 70점대이면 별7개, 80점대이면 별8개, 90점대이면 별9개 , 100점이면 별10개  }
		star=process_star(77);
		
		
        ///////////////////////(4) 출력
        //  process_show(name, kor, eng, math, total, avg, pass, jang, star);
		// public static 리턴값 메서드명(재료){ 할일 }
		// public static 리턴값 process_show(재료){ 할일 }
		
		
 System.out.println("::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::: ");
 System.out.println("\t이름\t국어\t영어\t수학\t총점\t평균\t합격여부\t장학생\t랭킹");
 System.out.println("--------------------------------------------------------------------------------------------");
 System.out.printf("%s\t%d\t%d\t%d\t%d\t%s\t%s\t%s\t%d\t%d",name, kor, eng, math, total, avg, pass, jang, star);
  System.out.println("-------------------------------------------------------------------------------------------");
	}// end main

}// end class


