-- 1인 미용실 대시보드에서 사용할 시술 카탈로그 테이블을 생성합니다.
CREATE TABLE SALON_SERVICE_ITEM (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR2(100 CHAR) NOT NULL,
    CATEGORY VARCHAR2(30 CHAR) NOT NULL,
    DURATION_MINUTES NUMBER NOT NULL,
    PRICE NUMBER(10, 2) NOT NULL,
    DESCRIPTION VARCHAR2(255 CHAR) NOT NULL,
    IMAGE_URL VARCHAR2(500 CHAR) NOT NULL,
    ADDITIONAL_IMAGE_URLS VARCHAR2(1500 CHAR),
    TOP_RANK NUMBER
);

-- 첫 예약 고객의 기본 정보를 저장하는 고객 테이블입니다.
CREATE TABLE SALON_CUSTOMER (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR2(80 CHAR) NOT NULL,
    EMAIL VARCHAR2(120 CHAR) NOT NULL,
    PHONE VARCHAR2(30 CHAR) NOT NULL,
    SOCIAL_PROVIDER VARCHAR2(30 CHAR),
    SOCIAL_ID VARCHAR2(160 CHAR),
    CREATED_AT TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP NOT NULL
);

-- 고객 예약과 원장 관리 화면에서 사용할 예약 테이블을 생성합니다.
CREATE TABLE SALON_RESERVATION (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    SERVICE_ID NUMBER NOT NULL,
    CUSTOMER_ID NUMBER NOT NULL,
    CUSTOMER_NAME VARCHAR2(80 CHAR) NOT NULL,
    CUSTOMER_EMAIL VARCHAR2(120 CHAR) NOT NULL,
    CUSTOMER_PHONE VARCHAR2(30 CHAR) NOT NULL,
    RESERVATION_DATE_TIME TIMESTAMP NOT NULL,
    NO_SHOW_POLICY_AGREED NUMBER(1) DEFAULT 1 NOT NULL,
    STATUS VARCHAR2(20 CHAR) NOT NULL,
    MEMO VARCHAR2(500 CHAR),
    CONSTRAINT FK_SALON_RESERVATION_SERVICE
        FOREIGN KEY (SERVICE_ID) REFERENCES SALON_SERVICE_ITEM (ID),
    CONSTRAINT FK_SALON_RESERVATION_CUSTOMER
        FOREIGN KEY (CUSTOMER_ID) REFERENCES SALON_CUSTOMER (ID)
);

-- 원장이 지정한 특정 휴무일을 저장하는 테이블입니다.
CREATE TABLE SALON_HOLIDAY (
    HOLIDAY_DATE DATE PRIMARY KEY,
    REASON VARCHAR2(200 CHAR)
);
