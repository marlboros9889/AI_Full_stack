<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.marinboy.dao.AuthDao">
    <select id="findByCredentials" resultType="com.marinboy.dto.UserDto">
        SELECT USERNAME AS username, NAME AS name, EMAIL AS email, PHONE AS phone, ROLE AS role
        FROM MB_USER WHERE USERNAME = #{username} AND PASSWORD = #{password}
    </select>
</mapper>
